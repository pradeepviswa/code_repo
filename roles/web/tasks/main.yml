---
# tasks file for web
- name: update cache
  apt:
    update_cache: yes
- name: Install apache web server
  apt:
    name: "{{ service_name }}"
    state: present
- name: Start and Enable apache service
  service:
    name: "{{ service_name }}"
    state: started
    enabled: yes


